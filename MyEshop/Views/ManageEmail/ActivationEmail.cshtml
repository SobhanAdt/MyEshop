@using System.Configuration
@model DataLayer.Users
<div style="direction: rtl">
    <h2>@Model.UserName عزیز </h2>
    <p>با تشکر از ثبت نام شما ، برای فعال سازی حساب کاربری خود بر روی لینک زیر کیلک کنید.</p>
    <p><a href="@ConfigurationManager.AppSettings["MyDomain"]/Account/ActiveUser/@Model.ActiveCode">لینک فعال سازی</a></p>
</div>
